# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules/
dist/
dist-ssr/
.vite/
*.local

# Never commit secrets — Gist PAT lives in browser localStorage only,
# but keep this here as a guardrail in case a .env sneaks in later.
.env
.env.local

# The owner's real schedule is personal data and lives outside the repo.
# This is a guardrail for the case where an exported backup gets dropped
# into the project directory while debugging an import — the repo is
# public, so a stray one would publish their daily routine.
cadence-backup-*.json
*.ics

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.DS_Store
Thumbs.db
